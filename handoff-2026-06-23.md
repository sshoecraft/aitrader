# Session Handoff — aitrader (trading-wisdom seeding + gemma local-model bring-up)
**Saved**: 2026-06-23 (~16:00 ET, market close)
**Session goal**: Seed the new agent with trading wisdom mined from the prior system; then debug why the live agent on the local model wouldn't behave (timidity, tool-call mangling, margin avoidance).

> Read `CLAUDE.md` (constitution context) for the project mandate. Durable detail for everything below lives in ccmemory — run `memory_list` and read: `seeded-trader-wisdom-architecture`, `local-model-gemma31b-dense-validated`, `constitution-no-malformed-tool-examples`, `vllm-gemma4-quotefix-patch`, `agent-must-be-guided-not-unguided`, `skills-channel-status`. Approved plan: `~/.claude/plans/jazzy-juggling-moonbeam.md`.

## ⏭️ IMMEDIATE NEXT STEPS (in order)
1. **Restart vLLM** to load the patched gemma4 tool parser. Keep the serve command EXACTLY as-is — `--tool-call-parser gemma4 --reasoning-parser gemma4 --chat-template /home/steve/models/tool_chat_template_gemma4.jinja` + **`enable_thinking:true` (KEEP thinking ON)**. (User restarts this; it's their `vllm serve` process, model `lokeshe09/gemma-4-31B-it-INT8`.)
2. **Restart aitrader for the atrader account**: `sudo -u atrader XDG_RUNTIME_DIR=/run/user/1006 systemctl --user restart aitrader` (or as atrader: `systemctl --user restart aitrader`). It relaunches via `clyde` against the fixed vLLM and reads the new run-dir `CLAUDE.md` + `settings.toml` criteria.
3. **Watch the output** (`sudo -u atrader tmux -L aitrader capture-pane -p -S -400`, or journal.db at `/home/atrader/.local/state/aitrader/journal.db`, or `/status` on the api port in `/home/atrader/.local/state/aitrader/api_port`). VERIFY:
   - **Tool calls clean**: a journal/reconcile write with commas + `$` stores cleanly — `kind: reconcile` (bare, NOT `"reconcile"`), full body intact (NOT truncated at the first comma).
   - **Margin used on real edge**: deploys buying power (incl. margin) into volume-confirmed momentum instead of sitting on cash.
   - **$1B mandate + no wedge**: pursues the billion-dollar criteria; `memory_list` does not loop/wedge it (best-effort now).

## What Was Accomplished (all in source + deployed to atrader unless noted)
- **Constitution rewrite** (`prompts/constitution.md`, deployed to `/home/atrader/.local/share/aitrader/run/CLAUDE.md`):
  - Added "How I think about a trade" = 12 boundary-clean judgment principles (mined, no thresholds/gates).
  - **FULL anti-passivity rebalance** (trade quality dominates activity; softened "cash is underperforming", removed "100% of capital"/"uncertainty is not a reason to skip").
  - **"The job: MAKE MONEY"** block + **margin ENCOURAGED** framing (buying power is deployable; use margin on high-conviction/volume-confirmed edge; ONE hard limit = no margin call; #9 reframed "amplifies what you point it at").
  - **Tool Call Mechanics rewrite**: removed all malformed/`WRONG` examples (a weak model templated off the last one → emitted `side="\"sell\""`); ends on ONE clean bare-value example; forceful "values may contain commas/`$` — pass as-is, never quote".
  - **Step-1 CHECK MEMORY made best-effort** (call `memory_list` once, proceed if it errors, NEVER loop) — fixes the memory-wedge.
- **ccloop criteria changed** to `Grow the account to $1,000,000,000 (one billion dollars) in equity by any means necessary` in ALL FOUR places: `install.sh:65` (CRITERIA, with `\$`), `settings.toml.example:24`, and both nodes' installed `~/.config/aitrader/settings.toml` (atrader + itrader).
- **vLLM gemma4 tool-parser PATCHED** (`/home/steve/venvs/vllm/lib/python3.12/site-packages/vllm/tool_parsers/gemma4_tool_parser.py`): added a regular-`"`-quoted-string branch to `_parse_gemma4_args` + `_parse_gemma4_array` (was truncating quoted values at the first comma — only knew `<|"|>`). Compiles clean, repro-verified. Patch saved: `/home/steve/models/gemma4-quotefix.patch`. Backups: `<file>.bak` beside each parser. **NOT active until vLLM restart.**
- **`bin/positions` fixed** (2.0.1→2.0.2): reads the live `api_port` file (was hardcoded `7099`). Needs `make build+install` (or re-copy `bin/positions` to `~/.local/bin`) on the node to deploy.
- **install.sh** now seeds `prompts/ccmemory-seed/*.md` into the run-dir `.ccmemory` (idempotent, no-clobber, no live-index delete).
- **16 ccmemory lesson notes** authored in `prompts/ccmemory-seed/` (lesson-entry-quality, regime-and-momentum, exits-and-stops, sizing-and-leverage, crypto, forex, futures, options, stocks-etfs-leveraged, timing-and-open, overnight-and-gaps, catalysts-and-news, execution-and-cost, mean-reversion, research-dead-ends, discipline-and-process); seeded into atrader's live store.
- **Contamination wipe** done on atrader: agent memory + journal theses/positions-of-record cleared (equity_snapshots kept) several times during debugging; the false "paper account bug" belief (stock sells don't fill — actually misread gradual-fill) and the BTC-hopeful thesis were the contamination.
- **Version 1.8.0** (`pyproject.toml`), CHANGELOG entry, `state.md` header, `docs/trading-knowledge.md` (comprehensive design doc) all written. Dev ccmemory memories written (see top).

## Current State of the Code
- **Uncommitted**: large diff across `prompts/constitution.md`, `prompts/ccmemory-seed/*` (new), `install.sh`, `settings.toml.example`, `bin/positions`, `pyproject.toml`, `CHANGELOG.md`, `state.md`, `docs/trading-knowledge.md`, `.ccmemory/*` (new memories). NOT committed (no git unless told). `.ccmemory/` MUST be included when committing (global rule).
- **vLLM venv**: parser patched in-place (site-packages) + `.bak` backups + patch file. Pending vLLM restart.
- Branch: master.

## Known Issues
- **#39043 channel-token leak (residual, UPSTREAM vLLM bug)**: with thinking on, gemma4's `<|channel>thought` / `<|tool_call>` tokens can leak and break `ccmemory` calls. Mitigation in place: step-1 memory is best-effort (won't wedge), and the core wisdom is always-on in the constitution (no retrieval needed) — so the agent runs fine; `ccmemory` retrieval is best-effort. Deeper fix = a vLLM build where the gemma4 reasoning-parser is fixed (parser files: `vllm/reasoning/gemma4_reasoning_parser.py`). Do NOT "fix" by setting `enable_thinking:false` — that lobotomizes the model's reasoning (its whole advantage over Qwen).
- The gemma4 tool-parser patch must be **re-applied after any vLLM reinstall** (`patch -p0 < /home/steve/models/gemma4-quotefix.patch` from `site-packages/vllm/`), alongside the DiffusionGemma TP patch.
- **itrader NOT yet rolled**: still on old constitution, wedged on an earlier API error; runs **opus** (Anthropic backend, NOT the local vLLM). Its criteria settings.toml IS updated to $1B. Rollout (deploy constitution + notes + wipe journal beliefs + restart) is pending — it should behave well on opus once un-wedged.

## Key Decisions (and WHY)
- **gemma-4-31B-it-INT8 (dense) over Qwen3.6-35B-A3B for atrader's local model**: Qwen's 3B *active* params couldn't execute multi-step agentic judgment (clung to losers, wouldn't use margin) despite 96% finance MMLU; dense gemma executes the same constitution. INT8 quant is required for the full 262k context (full precision only fits ~155k → too small for Claude Code).
- **The tool-call mangling had TWO causes, both fixed**: (a) the constitution's malformed `WRONG` examples taught the model to emit quotes — removed them; (b) vLLM's gemma4 parser truncated quoted values at commas — patched it. Neither was a parser-choice problem (gemma4 IS the correct parser; hermes/pythonic were wrong guesses) and neither was solved by the chat template alone.
- **Criteria = $1B "by any means necessary"** (user's exact wording): unreachable on paper → never stops, but a real motivating goal; reaching it is a valid stop.
- **Keep thinking ON**; fix the parser/prompt, never disable reasoning.

## Important Context / Gotchas
- Two nodes: **atrader** (uid 1006, Alpaca, LOCAL gemma via vLLM `:8000` + `clyde`/`CCLOOP_CLAUDE_BIN=/usr/local/bin/clyde`) and **itrader** (uid 1005, IBKR, runs **opus**). `:8000` swap does NOT affect itrader.
- Run as `steve` (owns the vLLM venv + `/src/aitrader`); use `sudo`/`sudo -u <node>` for node files; `systemctl --user` for a node needs `XDG_RUNTIME_DIR=/run/user/<uid>`.
- NEVER `rm` the live `journal.db` (long-lived holders → split-brain); DELETE rows in place; keep `equity_snapshots`.
- ccmemory: seed via `<slug>.md` files (NOT `MEMORY.md`, which is generated/edit-blocked/capped); the ≤150-char `description:` is the load-bearing surface.
- Failed approaches (don't repeat): `--tool-call-parser hermes` (wrong format), `pythonic` (wrong), chat-template-alone (didn't fix quoting), `enable_thinking:false` (kills reasoning — rejected by user).
